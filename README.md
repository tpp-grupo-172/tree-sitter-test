# tree-sitter-test

Para correr el programa, ubicar el archivo a analizar dentro del directorio *input-files* y ejecutar:

```cargo run -- <nombre_del_archivo>```

Por el momento solo hay soporte para Python.